{
  "name": "Python 3",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",

  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    },
    "codespaces": {
      "openFiles": ["README.md", "streamlit_app/streamlit_app.py"]
    }
  },

  /* Runs ONCE after the container is created (not every open) */
  "postCreateCommand": "if [ -f packages.txt ]; then sudo apt-get update && xargs -a packages.txt sudo apt-get install -y && sudo apt-get clean && rm -rf /var/lib/apt/lists/*; fi; if [ -f requirements.txt ]; then pip install -r requirements.txt; fi; pip install streamlit; echo '✅ Packages & requirements installed once'",

  /* Starts your app when you attach (fast, no heavy installs) */
  "postAttachCommand": {
    "server": "streamlit run streamlit_app/streamlit_app.py --server.enableCORS false --server.enableXsrfProtection false"
  },

  "portsAttributes": {
    "8501": { "label": "Application", "onAutoForward": "openPreview" }
  },
  "forwardPorts": [8501]
}
